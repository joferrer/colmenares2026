---
import Intro from "./sections/Intro.astro";
import SocialNetworks from "./ui/SocialNetworks.astro";
---

<div>
    <SocialNetworks />
    <Intro />
    <section
        class="relative flex flex-col max-w-7xl mx-auto bg-(--cd-oscuro) rounded-2xl z-50 p-5"
    >
        <div>
            <h1 class="text-4xl md:text-7xl font-extrabold text-(--cd-azul)">
                En que creemos
            </h1>
        </div>
        <div class="mt-10">
            <p class="text-xl md:text-2xl text-white">
                No tengo perra idea en que cree esta señora asi que no se que
                puede ir aquí
            </p>
        </div>
    </section>

    <section class="segmento mt-10 p-5">
        <div>
            <h1 class="text-4xl md:text-7xl font-extrabold text-(--cd-azul)">
                Enterate de lo que hacemos!
            </h1>
        </div>
        <div>
            <p class="text-xl md:text-2xl">
                En teoria aquí va lo de noticias de la campaña
            </p>
        </div>
    </section>

    <section class="segmento">
        <div>
            <h1 class="text-4xl md:text-7xl font-extrabold text-(--cd-azul)">
                Te necesitamos!!
            </h1>
            <p class="text-2xl">Y es este es el formulario de voluntariado</p>
        </div>
    </section>
</div>

<style is:global>
    :root {
        --cd-amarillo: #fec900;
        --cd-azul: #63b9e9;
        --cd-rojo: #d81a10;
        --cd-oscuro: #253e6b;
    }

    .segmento {
        position: relative;
        max-width: 80rem;
        margin: 2rem auto;
    }
</style>
