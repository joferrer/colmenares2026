---
import Layout from "../layouts/Layout.astro";
import FotoIntroEstefania from "../assets/fotos/COLMENARES_BLANCA.webp";
import IconoLibro from "../assets/iconos/ICONOS_INDIVIDUALES-19.png";
import FotoEstefaniaNegro from "../assets/fotos/COLMENARES_NEGRO.webp";

import { Image } from "astro:assets";
import { socials } from "../helpers/redes";
import Footer from "../components/ui/Footer.astro";
import Dialog from "../components/ui/modal/Dialog.astro";
---

<Layout>
    <!-- INTRO -->
    <section class="px-2 pt-5 lg:mt-20 mx-auto max-w-4xl">
        <div class="grid grid-cols-2 md:grid-cols-3 gap-3 md:gap-2">
            <div
                class="col-span-2 md:col-span-1 rounded-2xl md:shadow-md w-full max-md:rounded-full max-h-80 flex justify-center items-center"
            >
                <Image
                    src={FotoIntroEstefania}
                    alt=""
                    class:list={[
                        "max-md:rounded-full max-md:w-90 shadow-md md:shadow-none w-full h-full object-cover object-top",
                    ]}
                />
            </div>

            <div
                class="col-span-2 max-sm:py-5 flex flex-col justify-center px-2 rounded-2xl text-center shadow-md"
            >
                <h1 class="homey-title text-6xl text-(--cd-oscuro)">
                    Estefania Colmenares
                </h1>

                <p
                    class="uppercase tracking-widest text-sm text-gray-500 mt-1 mb-2"
                >
                    Aspirante al Senado · Colombia 2026
                </p>

                <p class="text-gray-700 leading-relaxed">
                    Creo en una política que escucha, que explica y que
                    construye desde la dignidad de las personas. Mi compromiso
                    es representar a quienes durante años no han tenido voz en
                    el Congreso.
                </p>

                <div class="flex justify-center gap-6 pt-4">
                    {
                        socials.map(({ name, href, icon }) => (
                            <a
                                href={href}
                                aria-label={name}
                                class="text-gray-600 text-4xl w- hover:text-(--cd-oscuro) transition-colors"
                            >
                                <Fragment set:html={icon} />
                            </a>
                        ))
                    }
                </div>
            </div>

            <div
                id="modal-academica-open"
                class="col-span-1 rounded-2xl shadow-md p-5 relative cursor-pointer"
            >
                <Dialog id="modal-academica" titulo="Academica">
                    <div>
                        <h3>Texto de prueba</h3>
                        <p>
                            Lorem ipsum dolor sit amet consectetur adipisicing
                            elit. Unde temporibus voluptate odit cupiditate at
                            ipsam laborum, aliquam distinctio. Itaque fugiat
                            unde fugit placeat at aliquid, recusandae fuga!
                            Ratione, sit fugit!
                        </p>
                    </div>
                </Dialog>
                <div
                    class="absolute top-0 left-0 w-full h-full -z-20 overflow-hidden"
                >
                    <div
                        class="absolute col-span-2 md:col-span-3 text-5xl md:text-4xl text-(--cd-oscuro) font-extralight homey-title italic -rotate-30 left-0 top-2"
                    >
                        Creo en
                    </div>
                    <Image
                        src={IconoLibro}
                        alt=""
                        class="w-full h-full md:w-34 md:h-34 absolute object-cover opacity-10 md:opacity-50 md:-top-8 md:-left-5"
                    />
                </div>
                <div
                    class="text-center min-h-30 flex items-center justify-center"
                >
                    <span
                        class="font-extralight text-(--cd-oscuro) italic homey-title text-7xl"
                    >
                        La Verdad
                    </span>
                </div>
            </div>
            <div
                class="col-span-1 rounded-2xl shadow-md p-5 relative cursor-pointer"
            >
                <div
                    class="absolute top-0 left-0 w-full h-full -z-20 overflow-hidden"
                >
                    <Image
                        src={IconoLibro}
                        alt=""
                        class="w-full h-full md:w-34 md:h-34 absolute object-cover opacity-10 md:opacity-50 md:-top-8 md:-left-5"
                    />
                </div>
                <div
                    class="text-center min-h-30 flex items-center justify-center"
                >
                    <span class="text-2xl font-bold text-(--cd-oscuro)"
                        >Profesional</span
                    >
                </div>
            </div>
            <div
                class="col-span-2 md:col-span-1 rounded-2xl shadow-md p-5 relative cursor-pointer"
            >
                <div
                    class="absolute top-0 left-0 w-full h-full -z-20 overflow-hidden"
                >
                    <Image
                        src={IconoLibro}
                        alt=""
                        class="w-full h-full md:w-34 md:h-34 absolute object-cover opacity-10 md:opacity-50 md:-top-8 md:-left-5"
                    />
                </div>
                <div
                    class="text-center min-h-30 flex items-center justify-center"
                >
                    <span class="text-2xl font-bold text-(--cd-oscuro)"
                        >Comprometida</span
                    >
                </div>
            </div>
        </div>
    </section>

    <Footer />
</Layout>

<script>
    const modalAcademica = document.getElementById("modal-academica");
    const modalProfecional = document.getElementById("modal-profecional");
    const modalComprometida = document.getElementById("modal-comprometida");
</script>

<style>
    :root {
        --cd-amarillo: #fec900;
        --cd-azul: #63b9e9;
        --cd-rojo: #d81a10;
        --cd-oscuro: #253e6b;
    }

    @font-face {
        font-family: "Homey Feeling";
        src: url("/font/HomeyFeeling.otf") format("opentype");
        font-display: swap;
    }

    .homey-title {
        font-family: "Homey Feeling";
    }

    /* FOTO: empieza MUY TARDE */
    .foto-lenta {
        animation: fadeInSlow linear both;
        animation-timeline: view();
        animation-range: entry 60% cover 85%;
    }

    @keyframes fadeInSlow {
        from {
            opacity: 0;
            scale: 0.92;
        }
        to {
            opacity: 1;
            scale: 1;
        }
    }

    /* TEXTO: casi imperceptible */
    .texto-lento {
        animation: textReveal linear both;
        animation-timeline: view();
        animation-range: entry 20% cover 50%;
    }

    @keyframes textReveal {
        from {
            opacity: 0;
            transform: translateY(24px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>
